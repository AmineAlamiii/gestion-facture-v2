# Variables d'environnement pour Vercel
# Copiez ces variables dans Vercel Dashboard > Settings > Environment Variables

# Base de données PostgreSQL (OBLIGATOIRE)
# Remplacez par l'URL réelle de votre base de données PostgreSQL
DATABASE_URL=postgresql://user:password@host:5432/database?schema=public

# Environnement
NODE_ENV=production

# Frontend URL (optionnel - pour CORS)
# Sera automatiquement détecté par Vercel, mais vous pouvez le définir manuellement
FRONTEND_URL=https://votre-app.vercel.app

# URLs supplémentaires pour CORS (optionnel)
# Séparez par des virgules si vous avez plusieurs domaines
FRONTEND_URLS=https://votre-domaine.com,https://www.votre-domaine.com

